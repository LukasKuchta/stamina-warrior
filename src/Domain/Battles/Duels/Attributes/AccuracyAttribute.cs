namespace Domain.Battles.Duels.Attributes;

public sealed record AccuracyAttribute : GameAttributeBase
{
    internal AccuracyAttribute(GameAttributeBase original) : base(original)
    {
    }
}
