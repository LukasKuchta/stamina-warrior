namespace Domain.Battles.Duels.Attributes;

public sealed record StrentghAttribute : GameAttributeBase
{
    internal StrentghAttribute(GameAttributeBase original) : base(original)
    {
    }
}
